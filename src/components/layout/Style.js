import { withStyles } from '@material-ui/core/styles';

const styles = {
  linkList: {
    display: 'flex',
  },
  linkListItem: {
    margin: '0 .5em',
  },
  linkListItemLink: {
    color: '#FFF'
  }
};

export default withStyles(styles);